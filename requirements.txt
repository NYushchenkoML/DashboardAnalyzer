# Зависимости для анализаторов (выполняются на клиенте через Pyodide)
# pandas и numpy устанавливаются автоматически в Pyodide
# Остальные зависимости не требуются, так как анализ выполняется на клиенте
